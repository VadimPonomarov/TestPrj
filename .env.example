# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-change-this-to-a-secure-key-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
POSTGRES_DB=mydb
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Optional: Email Settings (if needed)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=your-email-host
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password

# Optional: Caching (uncomment and configure if needed)
# CACHE_URL=redis://redis:6379/0

# Optional: Celery (if using Celery for background tasks)
# CELERY_BROKER_URL=redis://redis:6379/1
# CELERY_RESULT_BACKEND=redis://redis:6379/2

# Application Specific Settings
# Add any custom settings for your application below

PLAYWRIGHT_PROXY_SERVER=
PLAYWRIGHT_PROXY_USERNAME=
PLAYWRIGHT_PROXY_PASSWORD=

SELENIUM_PROXY_SERVER=
